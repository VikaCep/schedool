<?php

class Subject_classesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('subject_classes')->truncate();

		$subject_classes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('subject_classes')->insert($subject_classes);
	}

}
