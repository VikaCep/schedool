<?php

class SubjectclassesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('subjectclasses')->truncate();

		$subjectclasses = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('subjectclasses')->insert($subjectclasses);
	}

}
