<?php

class UsersubjectexamsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('usersubjectexams')->truncate();

		$usersubjectexams = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('usersubjectexams')->insert($usersubjectexams);
	}

}
