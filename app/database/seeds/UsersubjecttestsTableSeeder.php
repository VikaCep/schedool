<?php

class UsersubjecttestsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('usersubjecttests')->truncate();

		$usersubjecttests = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('usersubjecttests')->insert($usersubjecttests);
	}

}
